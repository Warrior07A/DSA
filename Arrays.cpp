//Array is a collection of similar kind of data stored in a continuous location

//CREATING AN ARRAY TO STORE 100 VARIABLES IN MEMORY
int S[100];   
//indexing will begin from 0 to N-1
//Initially the blocks will contain some random value -> GARBAGE VALUE

//setting/UPDATING value at index 5=10
S[5]=10; 

[10,"abc",true]//VALID IN JS~~~INVALID IN CPP

[1,2,4,4,5,] //valid in cpp(type =samee)

//swap
int main(){
  int x = 5;
  int y = 10;

  std::swap(x, y);

  // After swap:
  // x is now 10
  cout<<x<<y;
  
  // y is now 5

  //SORTING/////////////////////////////
 // inbuilt function
  //first index...last index
 sort(A[0],A[N]);

 //selection sort \...............................................................


 //CONSTANTS IN CPP
 //INT_MIN/INT_MAX/LONG_MAX/LONG_MIN/

 //NESTED LOOPS ITERATION
 for(int i=0;i<N;i++){
  for(int j=i+1;j<N;j++){         //this way u can get (0,1)(0,2)(0,3).....(0,N)...
                                  //then (1,1)(1,2)(1,3)..........(1,N) tilll(N,N)
  }
 }

 